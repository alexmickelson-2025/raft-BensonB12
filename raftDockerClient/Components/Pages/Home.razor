@page "/"
@rendermode InteractiveServer

<PageTitle>Home</PageTitle>

<h1>Hello, world!</h1>

@foreach (string url in Urls)
{
  <div>@url</div>
}

@code {
  HttpClient http = new HttpClient();

  string[] Urls = Environment.GetEnvironmentVariable("NODE_URLS").ToString().Split(',');
}